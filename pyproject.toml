[project]
name = "aoc25"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jinja2>=3.1.6",
    "pytest>=9.0.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
